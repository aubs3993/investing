.env
.claude/
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
dist/
*.egg-info/
.venv/
venv/
env/
.pytest_cache/
.mypy_cache/
.ipynb_checkpoints/

# Throwaway probe scripts written during agent sessions — never commit.
.tmp_probes/

# Office lock/temp files (created while a doc is open in Excel/Word/PowerPoint)
~$*.xlsx
~$*.xlsm
~$*.docx
~$*.pptx

macro/output/**/*
companies/output/**/*
!macro/output/**/
!companies/output/**/
!**/.gitkeep
!macro/output/**/*.png

# Override: track the HY OAS archive CSV. FRED restricted BAMLH0A0HYM2 to a
# rolling 3-year window in April 2026, so we accumulate our own daily history
# here. This is the only xlsx/csv data file under macro/output/ that is tracked.
!macro/output/credit_spreads/hy_oas_archive.csv
